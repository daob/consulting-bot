The application creates `data/` here at runtime, holding one JSON file per
session and one Markdown transcript per finished consultation. Both are
git-ignored. Delete the directory to reset; `make clean` does it locally.
